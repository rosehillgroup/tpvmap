---
import Layout from '../layouts/Layout.astro';
import UploadForm from '../components/UploadForm';
---

<Layout title="TPV Match - Upload Design">
	<div class="page-wrapper">
		<!-- Header Navigation -->
		<header class="site-header">
			<div class="header-content">
				<div class="logo">
					<svg width="32" height="32" viewBox="0 0 24 24" fill="none">
						<circle cx="8" cy="8" r="3" fill="var(--color-accent)"/>
						<circle cx="16" cy="8" r="3" fill="var(--color-primary)"/>
						<circle cx="12" cy="16" r="3" fill="var(--color-secondary)"/>
					</svg>
					<span>TPV Match</span>
				</div>
				<nav class="header-nav">
					<a href="#how-it-works">How it works</a>
					<a href="#about">About</a>
					<a href="https://rosehillpolymers.com" target="_blank" rel="noopener">Rosehill Polymers</a>
				</nav>
			</div>
		</header>

		<main class="main-content">
			<!-- Decorative background elements -->
			<div class="background-decoration">
				<div class="shape shape-1"></div>
				<div class="shape shape-2"></div>
				<div class="shape shape-3"></div>
			</div>

			<div class="container">
				<!-- Enhanced Hero Section -->
				<section class="hero">
					<div class="hero-badge">🎨 Professional Colour Matching</div>
					<h1 class="hero-title">
						Match Your Design to 
						<span class="gradient-text">Perfect TPV Blends</span>
					</h1>
					<p class="hero-description">
						Upload your design and let our advanced colour analysis find the exact 
						Rosehill TPV combinations to bring your vision to life. Trusted by designers 
						and manufacturers worldwide.
					</p>
					<div class="hero-stats">
						<div class="stat">
							<span class="stat-number">21</span>
							<span class="stat-label">Base Colours</span>
						</div>
						<div class="stat">
							<span class="stat-number"><1.0</span>
							<span class="stat-label">ΔE Accuracy</span>
						</div>
						<div class="stat">
							<span class="stat-number">∞</span>
							<span class="stat-label">Blend Options</span>
						</div>
					</div>
				</section>

				<!-- Upload Form Section -->
				<section class="upload-section">
					<div class="upload-card">
						<UploadForm client:load />
					</div>
				</section>

				<!-- How It Works Section -->
				<section id="how-it-works" class="how-it-works">
					<h2>How It Works</h2>
					<div class="steps">
						<div class="step">
							<div class="step-number">1</div>
							<div class="step-icon">📤</div>
							<h3>Upload Your Design</h3>
							<p>Support for PDF, PNG, JPG, and SVG files up to 50MB</p>
						</div>
						<div class="step">
							<div class="step-number">2</div>
							<div class="step-icon">🔍</div>
							<h3>Colour Analysis</h3>
							<p>Advanced algorithms extract and analyze your design colours</p>
						</div>
						<div class="step">
							<div class="step-number">3</div>
							<div class="step-icon">🎯</div>
							<h3>Perfect Matches</h3>
							<p>Get precise TPV blend recipes with industry-standard ΔE2000 accuracy</p>
						</div>
					</div>
				</section>
			</div>
		</main>

		<!-- Footer -->
		<footer class="site-footer">
			<div class="footer-content">
				<p>&copy; 2024 Rosehill Polymers. Professional TPV Solutions.</p>
				<div class="footer-links">
					<a href="https://rosehillpolymers.com/privacy" target="_blank" rel="noopener">Privacy</a>
					<a href="https://rosehillpolymers.com/contact" target="_blank" rel="noopener">Contact</a>
				</div>
			</div>
		</footer>
	</div>
</Layout>

<style>
	/* Page Structure */
	.page-wrapper {
		min-height: 100vh;
		display: flex;
		flex-direction: column;
		background: linear-gradient(180deg, #F8FAFC 0%, #F1F5F9 50%, #E2E8F0 100%);
	}

	.main-content {
		flex: 1;
		position: relative;
		overflow: hidden;
	}

	.container {
		max-width: 1200px;
		margin: 0 auto;
		padding: 0 2rem;
		position: relative;
		z-index: 1;
	}

	/* Header Styles */
	.site-header {
		background: rgba(255, 255, 255, 0.8);
		backdrop-filter: blur(10px);
		border-bottom: 1px solid var(--color-border-light);
		position: sticky;
		top: 0;
		z-index: 100;
		animation: slideDown 0.5s ease-out;
	}

	.header-content {
		max-width: 1200px;
		margin: 0 auto;
		padding: 1rem 2rem;
		display: flex;
		justify-content: space-between;
		align-items: center;
	}

	.logo {
		display: flex;
		align-items: center;
		gap: 0.75rem;
		font-family: var(--font-heading);
		font-size: 1.25rem;
		font-weight: 700;
		color: var(--color-primary);
	}

	.header-nav {
		display: flex;
		gap: 2rem;
		align-items: center;
	}

	.header-nav a {
		color: var(--color-text-light);
		text-decoration: none;
		font-weight: 500;
		transition: color 0.2s ease;
	}

	.header-nav a:hover {
		color: var(--color-accent);
	}

	/* Background Decoration */
	.background-decoration {
		position: absolute;
		width: 100%;
		height: 100%;
		overflow: hidden;
		pointer-events: none;
	}

	.shape {
		position: absolute;
		border-radius: 50%;
		filter: blur(80px);
		opacity: 0.3;
		animation: float 20s infinite ease-in-out;
	}

	.shape-1 {
		width: 600px;
		height: 600px;
		background: var(--color-accent);
		top: -300px;
		right: -200px;
	}

	.shape-2 {
		width: 400px;
		height: 400px;
		background: var(--color-primary);
		bottom: -200px;
		left: -100px;
		animation-delay: -5s;
	}

	.shape-3 {
		width: 300px;
		height: 300px;
		background: var(--color-secondary);
		top: 50%;
		left: 50%;
		transform: translate(-50%, -50%);
		animation-delay: -10s;
	}

	/* Hero Section */
	.hero {
		text-align: center;
		padding: 4rem 0 3rem;
		animation: fadeInUp 0.8s ease-out;
	}

	.hero-badge {
		display: inline-block;
		background: rgba(255, 107, 53, 0.1);
		color: var(--color-accent);
		padding: 0.5rem 1rem;
		border-radius: 20px;
		font-size: 0.875rem;
		font-weight: 500;
		margin-bottom: 1.5rem;
		animation: fadeIn 1s ease-out 0.2s both;
	}

	.hero-title {
		font-size: 3.5rem;
		font-weight: 700;
		line-height: 1.2;
		margin-bottom: 1.5rem;
		color: var(--color-primary);
		animation: fadeInUp 0.8s ease-out 0.3s both;
	}

	.gradient-text {
		background: linear-gradient(135deg, var(--color-accent) 0%, var(--color-secondary) 100%);
		-webkit-background-clip: text;
		background-clip: text;
		-webkit-text-fill-color: transparent;
		display: block;
		margin-top: 0.5rem;
	}

	.hero-description {
		font-size: 1.25rem;
		line-height: 1.8;
		color: var(--color-text-light);
		max-width: 700px;
		margin: 0 auto 3rem;
		animation: fadeInUp 0.8s ease-out 0.4s both;
	}

	.hero-stats {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: 2rem;
		max-width: 500px;
		margin: 0 auto;
		animation: fadeInUp 0.8s ease-out 0.5s both;
	}

	.stat {
		text-align: center;
		padding: 1rem;
		background: rgba(255, 255, 255, 0.5);
		backdrop-filter: blur(10px);
		border-radius: var(--radius-sm);
		border: 1px solid rgba(255, 255, 255, 0.3);
		transition: transform 0.2s ease;
	}

	.stat:hover {
		transform: translateY(-2px);
	}

	.stat-number {
		display: block;
		font-size: 2rem;
		font-weight: 700;
		color: var(--color-primary);
		font-family: var(--font-heading);
	}

	.stat-label {
		display: block;
		font-size: 0.875rem;
		color: var(--color-text-light);
		margin-top: 0.25rem;
	}

	/* Upload Section */
	.upload-section {
		padding: 2rem 0 4rem;
		animation: fadeInUp 0.8s ease-out 0.6s both;
	}

	.upload-card {
		background: white;
		border-radius: var(--radius);
		box-shadow: 0 10px 40px rgba(0, 0, 0, 0.1);
		padding: 3rem;
		max-width: 700px;
		margin: 0 auto;
		border: 1px solid var(--color-border-light);
		position: relative;
		overflow: hidden;
	}

	.upload-card::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		height: 4px;
		background: linear-gradient(90deg, var(--color-primary), var(--color-accent));
	}

	/* How It Works Section */
	.how-it-works {
		padding: 4rem 0;
		text-align: center;
		animation: fadeIn 0.8s ease-out 0.8s both;
	}

	.how-it-works h2 {
		font-size: 2.5rem;
		margin-bottom: 3rem;
		color: var(--color-primary);
		font-weight: 700;
	}

	.steps {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
		gap: 2rem;
		max-width: 1000px;
		margin: 0 auto;
	}

	.step {
		position: relative;
		background: white;
		padding: 2rem;
		border-radius: var(--radius);
		box-shadow: 0 5px 20px rgba(0, 0, 0, 0.08);
		border: 1px solid var(--color-border-light);
		transition: all 0.3s ease;
	}

	.step:hover {
		transform: translateY(-5px);
		box-shadow: 0 10px 30px rgba(0, 0, 0, 0.12);
	}

	.step-number {
		position: absolute;
		top: -15px;
		left: 50%;
		transform: translateX(-50%);
		background: var(--color-accent);
		color: white;
		width: 30px;
		height: 30px;
		border-radius: 50%;
		display: flex;
		align-items: center;
		justify-content: center;
		font-weight: 700;
		font-size: 0.875rem;
	}

	.step-icon {
		font-size: 3rem;
		margin-bottom: 1rem;
	}

	.step h3 {
		font-size: 1.25rem;
		margin-bottom: 0.5rem;
		color: var(--color-primary);
	}

	.step p {
		color: var(--color-text-light);
		font-size: 0.875rem;
		line-height: 1.6;
	}

	/* Footer */
	.site-footer {
		background: var(--color-surface);
		border-top: 1px solid var(--color-border-light);
		padding: 2rem 0;
		margin-top: 4rem;
	}

	.footer-content {
		max-width: 1200px;
		margin: 0 auto;
		padding: 0 2rem;
		display: flex;
		justify-content: space-between;
		align-items: center;
		color: var(--color-text-light);
		font-size: 0.875rem;
	}

	.footer-links {
		display: flex;
		gap: 2rem;
	}

	.footer-links a {
		color: var(--color-text-light);
		text-decoration: none;
		transition: color 0.2s ease;
	}

	.footer-links a:hover {
		color: var(--color-accent);
	}

	/* Animations */
	@keyframes slideDown {
		from {
			opacity: 0;
			transform: translateY(-20px);
		}
		to {
			opacity: 1;
			transform: translateY(0);
		}
	}

	@keyframes fadeIn {
		from {
			opacity: 0;
		}
		to {
			opacity: 1;
		}
	}

	@keyframes fadeInUp {
		from {
			opacity: 0;
			transform: translateY(30px);
		}
		to {
			opacity: 1;
			transform: translateY(0);
		}
	}

	@keyframes float {
		0%, 100% {
			transform: translateY(0) scale(1);
		}
		25% {
			transform: translateY(-20px) scale(1.05);
		}
		75% {
			transform: translateY(20px) scale(0.95);
		}
	}

	/* Mobile Styles */
	@media (max-width: 768px) {
		.header-nav {
			display: none;
		}

		.hero-title {
			font-size: 2.5rem;
		}

		.hero-description {
			font-size: 1.125rem;
		}

		.hero-stats {
			gap: 1rem;
		}

		.stat-number {
			font-size: 1.5rem;
		}

		.upload-card {
			padding: 2rem 1.5rem;
		}

		.steps {
			grid-template-columns: 1fr;
		}

		.footer-content {
			flex-direction: column;
			gap: 1rem;
			text-align: center;
		}
	}
</style>
